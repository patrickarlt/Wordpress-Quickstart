/*
Stop jQuery from conflicting with any other library and still use the $ shortcut 
Should only be enabled if another JS library like Prototype or MooTools is used.
*/

//$.noConflict();

jQuery(document).ready(function($) {
  //Code that uses jQuery's $ can follow here.
});





















